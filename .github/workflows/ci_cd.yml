name: continuous integration (ci)

on: [pull_request]

# To successfully find the files that are required for testing:
env:
  TEST_WORKSPACE: echo $GITHUB_WORKSPACE

jobs:
  ci:
    # Set up operating system
    runs-on: ubuntu-latest

    # Define job steps
    steps:
      - name: Set up Python 3.10.2
        uses: actions/setup-python@v2
        with:
          python-version: 3.10.2

      - name: Check-out repository
        uses: actions/checkout@v2

      - name: Install poetry
        uses: snok/install-poetry@v1

      - name: Install package
        run: poetry install

      - name: Run pylama to check src directory
        run: poetry run pylama --async src/

      - name: Run pylama on the tests directory
        run: poetry run pylama --async tests/

      - name: Test with pytest
        run: poetry run pytest -m minor --ignore-glob='test_*.py' .\tests\test_cases\ --cov=sc2egset_dataset --cov-report term-missing --cov=xml

      # - name: Use Codecov to track coverage
      #   uses: codecov/codecov-action@v2
      #   with:
      #     files: ./coverage.xml # coverage report

      - name: Build documentation
        run: poetry run make html --directory docs/

  # cd:
  #   # Only run this job if the "ci" job passes
  #   needs: ci

  #   # Only run this job if new work is pushed to "main"
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'

  #   # Set up operating system
  #   runs-on: ubuntu-latest

  #   # Define job steps
  #   steps:
  #   - name: Set up Python 3.10.2
  #     uses: actions/setup-python@v2
  #     with:
  #       python-version: 3.10.2

  #   - name: Check-out repository
  #     uses: actions/checkout@v2
  #     with:
  #       fetch-depth: 0

  #   - name: Install poetry
  #     uses: snok/install-poetry@v1

  #   - name: Install package
  #     run: poetry install

  #   - name: Use Python Semantic Release to prepare release
  #     env:
  #       # This token is created automatically by GH Actions
  #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #     run: |
  #         git config user.name github-actions
  #         git config user.email github-actions@github.com
  #         poetry run semantic-release publish

  #   - name: Publish to TestPyPI
  #     uses: pypa/gh-action-pypi-publish@release/v1
  #     with:
  #       user: __token__
  #       password: ${{ secrets.TEST_PYPI_API_TOKEN }}
  #       repository_url: https://test.pypi.org/legacy/

  #   - name: Test install from TestPyPI
  #     run: |
  #         pip install \
  #         --index-url https://test.pypi.org/simple/ \
  #         --extra-index-url https://pypi.org/simple \
  #         sc2_datasets

  #   - name: Publish to PyPI
  #     uses: pypa/gh-action-pypi-publish@release/v1
  #     with:
  #       user: __token__
  #       password: ${{ secrets.PYPI_API_TOKEN }}
